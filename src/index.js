import {
    Compression,
    Framework,
    Database,
    Octokit,
    Router,
    Axios,
    ORM
} from "./module.js";

import * as Fetch from "node-fetch";

export const Library = {
    Compression,
    Framework,
    Database,
    Octokit,
    Router,
    Fetch,
    Axios,
    ORM
};

export default Library;
